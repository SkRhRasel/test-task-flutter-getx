import 'package:flutter/material.dart';

class VSpacer0 extends StatelessWidget {
  const VSpacer0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 0,
    );
  }
}

class VSpacer1 extends StatelessWidget {
  const VSpacer1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 1,
    );
  }
}

class VSpacer2 extends StatelessWidget {
  const VSpacer2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 2,
    );
  }
}
class VSpacer3 extends StatelessWidget {
  const VSpacer3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 3,
    );
  }
}

class VSpacer5 extends StatelessWidget {
  const VSpacer5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 5,
    );
  }
}

class VSpacer8 extends StatelessWidget {
  const VSpacer8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 8,
    );
  }
}

class VSpacer10 extends StatelessWidget {
  const VSpacer10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}

class VSpacer15 extends StatelessWidget {
  const VSpacer15({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15,
    );
  }
}

class VSpacer20 extends StatelessWidget {
  const VSpacer20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}

class VSpacer25 extends StatelessWidget {
  const VSpacer25({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 25,
    );
  }
}

class VSpacer30 extends StatelessWidget {
  const VSpacer30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 30,
    );
  }
}

class VSpacer40 extends StatelessWidget {
  const VSpacer40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 40,
    );
  }
}

class VSpacer50 extends StatelessWidget {
  const VSpacer50({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 50,
    );
  }
}

class VSpacer60 extends StatelessWidget {
  const VSpacer60({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 60,
    );
  }
}

class VSpacer65 extends StatelessWidget {
  const VSpacer65({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 65,
    );
  }
}

class VSpacer80 extends StatelessWidget {
  const VSpacer80({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 80,
    );
  }
}

class VSpacer100 extends StatelessWidget {
  const VSpacer100({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 100,
    );
  }
}

class VSpacer400 extends StatelessWidget {
  const VSpacer400({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 400,
    );
  }
}

class HSpacer0 extends StatelessWidget {
  const HSpacer0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 0,
    );
  }
}

class HSpacer5 extends StatelessWidget {
  const HSpacer5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 5,
    );
  }
}

class HSpacer10 extends StatelessWidget {
  const HSpacer10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 10,
    );
  }
}

class HSpacer15 extends StatelessWidget {
  const HSpacer15({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 15,
    );
  }
}

class HSpacer20 extends StatelessWidget {
  const HSpacer20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 20,
    );
  }
}

class HSpacer30 extends StatelessWidget {
  const HSpacer30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 30,
    );
  }
}

class HSpacer40 extends StatelessWidget {
  const HSpacer40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 40,
    );
  }
}

class HSpacer50 extends StatelessWidget {
  const HSpacer50({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 50,
    );
  }
}
